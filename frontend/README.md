# MyContacts Frontend

This is a minimal React + Vite frontend for the MyContacts backend.

To install and run:

```bash
cd frontend
npm install
npm run dev
```

The frontend expects the backend to run on `http://localhost:5000`.
